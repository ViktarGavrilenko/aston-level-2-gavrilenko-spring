/*
package org.example.models;

import org.junit.jupiter.api.Test;

import java.util.List;

import static org.example.models.BuyerTest.getTempOrders;
import static org.junit.jupiter.api.Assertions.assertEquals;

class ItemTest {
    private final int id = 1;
    private final String name = "someName";
    private final int price = 10;
    public List<Order> orders = getTempOrders();

    @Test
    void getId() {
        Item item = new Item(id, name, price, orders);
        assertEquals(id, item.getId());
    }

    @Test
    void setId() {
        Item item = new Item();
        item.setId(id);
        assertEquals(id, item.getId());
    }

    @Test
    void getName() {
        Item item = new Item(id, name, price, orders);
        assertEquals(name, item.getName());
    }

    @Test
    void setName() {
        Item item = new Item();
        item.setName(name);
        assertEquals(name, item.getName());
    }

    @Test
    void getPrice() {
        Item item = new Item(id, name, price, orders);
        assertEquals(price, item.getPrice());
    }

    @Test
    void setPrice() {
        Item item = new Item();
        item.setPrice(price);
        assertEquals(price, item.getPrice());
    }

    @Test
    void getOrders() {
        Item item = new Item(id, name, price, orders);
        assertEquals(orders, item.getOrders());
    }

    @Test
    void setOrders() {
        Item item = new Item();
        item.setOrders(orders);
        assertEquals(orders, item.getOrders());
    }

}*/
